diff --git a/kicad_mcp/tools/pcb.py b/kicad_mcp/tools/pcb.py
--- a/kicad_mcp/tools/pcb.py
+++ b/kicad_mcp/tools/pcb.py
@@ -4906,6 +4906,10 @@
 def register(mcp: FastMCP) -> None:
     """Register PCB tools."""
 
+    from . import pcb_visual_review
+
+    pcb_visual_review.register(mcp)
+
     pcb_board_inspection.register(
         mcp,
         pcb_board_inspection.PcbBoardInspectionDependencies(
diff --git a/kicad_mcp/tools/router.py b/kicad_mcp/tools/router.py
--- a/kicad_mcp/tools/router.py
+++ b/kicad_mcp/tools/router.py
@@ -180,6 +180,10 @@
             "pcb_get_design_rules",
             "pcb_get_footprint_layers",
             "pcb_visual_qa",
+            "pcb_visual_review",
+            "pcb_visual_history",
+            "pcb_visual_get",
+            "pcb_visual_compare",
             "pcb_get_impedance_for_trace",
             "pcb_get_groups",
             "pcb_get_origin",
@@ -641,6 +645,10 @@
     "run_erc",
     "validate_footprints_vs_schematic",
     "pcb_visual_qa",
+    "pcb_visual_review",
+    "pcb_visual_history",
+    "pcb_visual_get",
+    "pcb_visual_compare",
     "dfm_run_manufacturer_check",
     "project_quality_gate",
     "lib_verify_component_contract",
diff --git a/kicad_mcp/capabilities.py b/kicad_mcp/capabilities.py
--- a/kicad_mcp/capabilities.py
+++ b/kicad_mcp/capabilities.py
@@ -411,4 +411,6 @@
 def _tier_for_tool(name: str, category: str) -> AccessTier:
+    if name in {"pcb_visual_review", "pcb_visual_get", "pcb_visual_compare"}:
+        return AccessTier.EXPORT
     if name == "export_manufacturing_package":
         return AccessTier.HUMAN_ONLY
     if _is_read_tool(name, category):
@@ -427,4 +429,8 @@
 def _runtime_for_tool(name: str, category: str, tier: AccessTier) -> RuntimeRequirement:
+    if name == "pcb_visual_review":
+        return RuntimeRequirement.KICAD_CLI
+    if name in {"pcb_visual_history", "pcb_visual_get", "pcb_visual_compare"}:
+        return RuntimeRequirement.NONE
     if name in {"sch_render_png", "sch_render_visual_diff", "sch_set_title_block_info"}:
         return RuntimeRequirement.NONE
     if category == "simulation" or name.startswith("sim_"):
